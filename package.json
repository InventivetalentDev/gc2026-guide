{
  "name": "gc2026-guide",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tools/build-site.sh",
    "check:client": "node --check js/queue.js && node --check js/app.js && node --check sw.js",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "test": "npm run build && npm run check:client && vitest run",
    "test:watch": "npm run build && vitest",
    "db:migrate:local": "wrangler d1 migrations apply gc2026-queues --local",
    "db:migrate:production": "wrangler d1 migrations apply gc2026-queues --remote",
    "db:migrate:staging": "wrangler d1 migrations apply gc2026-queues-staging --env staging --remote"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.20.3",
    "vitest": "4.1.10",
    "wrangler": "4.123.0"
  }
}
